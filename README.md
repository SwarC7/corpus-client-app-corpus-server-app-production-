# MyClientApp (React + Vite)

This project is a React client app built with [Vite](https://vitejs.dev/).  
It provides a minimal setup to get React working with hot module replacement (HMR) and ESLint rules for development.

---

## 🚀 Getting Started

### Prerequisites
- [Node.js](https://nodejs.org/) v18+ (LTS recommended)
- npm (comes with Node.js) or yarn

---

### Installation
Clone the repository and install dependencies:

```bash
git clone git@code.swecha.org:Swar/myclientapp.git
cd myclientapp
npm install

Start the development server:
npm run dev

Then open http://localhost:5173
